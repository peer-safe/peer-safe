import { Web3Auth } from "@web3auth/modal";

const SharedWithMe = ({ web3Auth }: { web3Auth: Web3Auth }) => {
  return (
    <div className="flex flex-1 flex-col gap-12">
      WIP
    </div>
  );
};

export default SharedWithMe;
